# BareaPlay 운영 저장소 작업 규칙

## 적용 범위

이 지침은 이 저장소 전체와 모든 하위 경로에 적용한다. 이 앱은 여러 사용자가 실제로 사용하는 운영 앱이며, GitHub에 push된 코드가 Vercel을 통해 운영 사이트에 배포되고 운영 데이터는 Firebase에 저장된다.

## 최우선 원칙

- 기존 Firebase 운영 데이터 보존을 모든 작업보다 우선한다.
- `players`, `attendance`, `expenses`, `incomes`, `votes`, `dailyMeetings`, `matchRecords` 및 기타 기존 컬렉션과 데이터를 임의로 삭제하거나 초기화하지 않는다.
- 운영 Firebase에 테스트 데이터를 생성하지 않는다.
- 데이터 삭제, 일괄 수정, 마이그레이션은 사용자의 명시적인 승인 없이는 실행하지 않는다.
- 기존 Firestore collection(컬렉션) 이름, 문서 ID 규칙, 필드 구조를 임의로 변경하지 않는다.
- 기존 사용자 데이터와 하위 호환성을 유지한다.
- 데이터 구조 변경이 필요하면 구현 전에 기존 데이터에 미치는 영향, 호환 전략, 마이그레이션 절차 및 실패 시 복구 방법을 먼저 보고하고 승인을 받는다.
- Firebase 설정값, API 설정 내용, 인증정보, 토큰 및 비밀값을 출력하거나 외부에 노출하지 않는다.

## 코드 변경 절차

- 기능은 한 번에 하나의 독립된 단위로 수정한다.
- 수정 전에 관련 코드, Firestore 읽기·쓰기 경로, 연관 기능 및 Service Worker 캐시 영향을 먼저 확인한다.
- 요청 범위와 무관한 HTML, CSS, JavaScript 또는 설정 파일을 함께 수정하지 않는다.
- 수정 후 가능한 JavaScript 구문 검사와 기존 기능 영향 점검을 수행한다.
- 자동 테스트 환경이 마련되기 전에는 운영 Firebase에 쓰기가 발생하는 테스트를 하지 않는다.
- Service Worker 캐시 이름이나 캐시 대상 URL을 변경할 때는 기존 사용자가 새 버전을 정상적으로 받는지, 모듈의 `?v=` 버전과 캐시 목록이 일치하는지 확인한다.

## Git 및 배포 안전 규칙

- 사용자가 승인하기 전에는 commit, push 또는 Vercel 배포를 하지 않는다.
- `reset`, `revert`, `clean`, force push를 실행하지 않는다.
- 현재 사용 중인 `master` 브랜치와 기존 Git 이력을 보존한다.
- 기존 작업 트리의 사용자 변경사항을 덮어쓰거나 제거하지 않는다.
- Git 작업 전후에 branch, HEAD 및 `git status`를 확인하고 변경 파일 범위가 요청과 일치하는지 보고한다.

## 데이터 위험이 있는 기존 동작

- 선수 엑셀 업로드는 현재 `players` 컬렉션 전체를 삭제한 후 다시 생성하는 방식이므로 운영 데이터로 실행하거나 수정할 때 특별한 승인이 필요하다.
- 출석 저장은 선택한 날짜에서 체크 해제된 `attendance` 문서를 삭제할 수 있다.
- 회비 범위 삭제, 지난 투표 삭제, 선수·지출·수입·장소 삭제는 영구 삭제 경로를 포함한다.
- 날짜별 팀·라인업 저장은 `dailyMeetings/{date}` 문서를 전체 덮어쓴다.
- 위 동작을 변경하거나 실행하기 전에는 `docs/CURRENT_STATUS.md`와 `docs/DATA_MODEL.md`를 확인한다.
